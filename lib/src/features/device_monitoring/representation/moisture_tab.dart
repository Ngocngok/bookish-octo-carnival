import 'package:flutter/material.dart';

class MoistureTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BURGER TAB"),
      ),
    );
  }
}